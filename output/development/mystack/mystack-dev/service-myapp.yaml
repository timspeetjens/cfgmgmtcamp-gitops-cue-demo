---
spec:
  ports:
    - port: 80
      targetPort: http
      name: http
  selector:
    app.kubernetes.io/name: myapp
    app.kubernetes.io/instance: dev
  type: ClusterIP
metadata:
  name: myapp
  namespace: mystack-dev
kind: Service
apiVersion: v1
